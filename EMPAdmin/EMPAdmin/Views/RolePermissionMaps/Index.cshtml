@model IEnumerable<uTax.EntityFramework.Edmx.RolePermissionMap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusCodeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PermissionMaster.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleMaster.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusCodeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PermissionMaster.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleMaster.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
